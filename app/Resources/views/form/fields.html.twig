{% block form_row %}
    {% spaceless %}
        <div class="form-group">
            {{ form_label(form) }}
            {{ form_errors(form) }}
            <div class="col-sm-4">
            {{ form_widget(form) }}
                </div>
        </div>
    {% endspaceless %}
{% endblock form_row %}